# Flask 同步 API
flask>=3.0,<4.0

# 通义 / LangChain / LangGraph
langchain-core>=0.3,<0.4
langchain-community>=0.3,<0.4
langgraph>=0.2,<0.3
langgraph-checkpoint-postgres>=2.0,<3.0
dashscope>=1.20

# 图片处理
Pillow>=10.0

# Redis / RabbitMQ / Postgres / HTTP
redis>=5.0
pika==1.3.2
psycopg[binary]>=3.1
requests>=2.31
oss2>=2.18

# 配置文件
PyYAML>=6.0
