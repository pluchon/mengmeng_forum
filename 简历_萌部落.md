# 萌部落社区后端开发——面试复习与拆解指南

> **💡 复习建议**：
> 本指南专为实习生面试准备，将内容拆分为**“第一部分：💬 面试通关 Q&A 卡片”**与**“第二部分：💻 核心源码对照”**。
> 面试前建议重点背诵**第一部分**的白话逻辑，需要对照底层实现时再查阅**第二部分**的精简代码。

---

# 第一部分：💬 面试通关 Q&A 卡片

## 模块零：项目架构与数据库设计

#### Q1：你这个项目整体是怎么设计的？
* **为什么拆模块**：项目拆分为 `backend`（Spring Boot 单体后端服务）和 `ai-server`（Python 异步微服务）。
  因为 AI 多模态审核和 RAG 推荐属于 CPU 和网络 IO 密集型操作，且 Python 的 AI 生态（如 LangGraph 等）更为成熟。如果把 AI 接口放在 Java 主站中同步运行，容易直接拖垮主站的 CPU 资源。拆分后，主站通过 RabbitMQ 与 AI 异步通信，主业务与 AI 任务互不干扰，性能更好。
* **Controller、Service、Mapper怎么划分**：
  我们严格遵守 `Controller -> Service -> Mapper` 三层架构，禁止跨层调用：
  * **Controller**：仅负责接收请求、轻量校验基本入参、返回统一的 Result 对象，绝不写业务逻辑。
  * **Service**：核心业务逻辑层，在这里处理锁、事务、保底过滤等复杂的业务控制。
  * **Mapper**：基于 MyBatis-Plus，只负责对 MySQL 数据库执行最基础的增删改查。
* **哪些地方体现了可维护性**：
  1. 状态数值全部封装在 Java 的 **Enum 枚举类**中，杜绝了硬编码魔法数字。
  2. 统一的用户会话和权限校验放在**拦截器（Interceptor）**中进行全局拦截，不需要在每个业务 Service 里重复编写鉴权代码。
  3. 业务代码与防刷等安全逻辑解耦，像验证码票据校验服务抽成了独立的公共类，哪里的接口需要用直接引入即可。

#### Q2：你的数据库是怎么设计的？
* **用户、帖子、评论、私信、积分流水怎么关联**：
  * **用户表（user）**：主键是自增 id，保存用户账号和哈希加密的手机号等。
  * **帖子表（article）**：外键 `user_id` 关联用户表，记录发帖人。
  * **评论表（comment）**：外键 `article_id` 关联帖子，外键 `user_id` 关联发评论的用户。
  * **私信表（message）**：有 `post_user_id`（发件人）和 `receive_user_id`（收件人）两个外键关联用户表的 id。
  * **积分流水表（points_log）**：外键 `user_id` 关联用户。每当发生积分变更，就在此表中插入一条流水。
* **为什么积分流水不直接存在用户表**：
  积分在社区里充当虚拟货币的角色，如果只在用户表保存一个余额数值，当出现积分异常或用户申诉时，我们无法进行追溯和对账。所以必须设立专门的 `points_log` 积分流水表，记录每一次变更的额度、变更类型（签到、中奖、扣积分等）以及变更后的快照余额，用来保障数据可审计和安全性。
* **哪些字段建立索引？为什么**：
  1. **唯一索引**：在 `user` 表的 `phone_hash`（手机号哈希）上建唯一索引，确保全局手机号唯一。
  2. **关联普通索引**：在 `article` 表的 `user_id`、`comment` 表的 `article_id`、`message` 表 of `receive_user_id` 以及 `points_log` 的 `user_id` 上建立单列普通索引。因为这些字段在分页拉取帖子、未读私信或评论时频繁用于 `WHERE` 或 `JOIN`，建索引能把全表扫描优化为快速的索引查找，避免数据库卡死。

---

## 模块一：验证码防刷

#### Q3：一次性票据和限流分别防御什么场景？
* **一次性票据**：防的是**“接口裸调”与“验证结果重放”**。防止黑客跳过前端滑块直接调用发短信接口，或者滑动一次成功后拿同一个成功凭证重复调用。
* **分层限流**：防的是**“滑块被自动工具破解”与“人工恶意刷量”**。即便滑块被识别攻破，限流作为第二道防线（60秒冷却+30分钟最多10次），能兜底保障短信网关不被耗尽。

#### Q4：票据存哪、用什么结构、怎么保证一次性？
* **存储与结构**：存放在 Redis 中，设置 2 分钟过期时间（TTL）。采用 **String（Key-Value）** 结构，Key 是随机的 UUID（防止暴力猜测），Value 是本次请求的业务目的（如 `SMS_SEND`），防止票据挪作他用。
* **如何保证一次性**：使用 **Lua 脚本**在 Redis 侧原子化地完成 `GET-比对-DEL`。因为 Redis 执行 Lua 脚本是单线程且原子性的，可以避免并发下多个请求同时 GET 并通过校验的**并发竞争问题**，真正做到“校验通过立即销毁”。

#### Q5：如果此时 Redis 挂了，验证码票据和限流怎么降级处理？
* **答**：如果 Redis 挂了没法写入，我们会进行 `try-catch` 降级，**临时改用服务器本机的内存（比如本地 Map）**来存储票据和限流计数。虽然在集群环境下没法共享，但至少能保证单台服务器的安全校验能正常运转，不会导致登录注册卡死。

---

## 模块二：并发抽奖控制

#### Q6：为什么积分和库存扣减要在同一个事务里？
* **答**：抽奖包含‘扣用户积分’和‘减奖品库存’两步。如果不放入同一个 `@Transactional` 事务中，一旦中途出现网络抖动或库存刚好归零，就会导致扣了积分却没发货，或者发了货却没扣积分的**数据不一致问题**。通过事务，如果其中一步失败，整笔操作回滚，确保钱货一致。

#### Q7：为什么扣减限量奖品要用条件更新，而不是先查后改？
* **答**：高并发下‘先 SELECT 查库存，再 UPDATE 减库存’会导致**超卖**（多个线程同时查到库存为 1，都校验通过，结果库存被减成负数）。
* 我们改用数据库层面的**条件更新（乐观锁）**：`UPDATE ... SET stock = stock - 1 WHERE id = #{id} AND stock > 0`。执行时不阻塞、不等待。如果库存为 0，更新影响行数返回 `0`，代码据此立即得知没抢到库存，完美规避超卖。

#### Q8：既然用了数据库条件更新，为什么不用 synchronized 或者是 Redis 分布式锁？
* **为什么不用 synchronized**：因为 `synchronized` 是单机锁，只对单台服务器的 JVM 进程有用。多实例集群部署下，不同服务器之间的线程无法同步，依然会导致超卖。
* **为什么不用 Redis 锁**：抽奖并发量高，如果加 Redis 分布式锁，所有抽奖请求都会在 Redis 侧排队，频繁获取锁/释放锁的网络开销极大，吞吐量骤降。而条件更新直接在数据库用乐观条件更新（不阻塞），性能要高得多。

#### Q9：扣库存失败后为什么要重试？重试的是什么？
* **答**：高并发竞争下，很多用户的扣库存操作会因为慢了一步而返回 0（失败）。如果直接给用户报错，用户体验会极差。
* 我们重试的是**“换奖重抽”的完整逻辑**：当扣减失败时，代码进入一个最大 64 次的循环，重新拉取当前还有可用库存的奖池，重新通过加权随机算法挑选另一个奖项，再次尝试扣减新奖项的库存，直到成功或达到次数上限。这保证了只要付了积分就一定能拿到有库存的奖品。

#### Q10：软保底和硬保底是怎么实现的？
* **硬保底（50抽必出）**：用户表里设计 `lottery_pity_draws` 字段记录连续没中大奖次数。事务中悲观锁锁定并读取该计数，若达到 50 次，在挑选奖品前，代码会**剔除所有普通奖品，只保留大奖子池**进行加权随机，从而保证这次必中大奖。
* **十连软保底**：在十连抽循环中，前 9 抽若均未命中稀有奖（大奖、小奖、VIP），第 10 抽挑选奖品时**过滤掉普通安慰奖，强制在稀有奖品池中加权随机**。

---

## 模块三：私信跨实例推送

#### Q11：单机 WebSocket 多实例集群部署下为什么会失效？
* **答**：因为 WebSocket Session 会话对象存在**各台服务器本机的 JVM 内存里**，两台机器之间的内存是相互隔离的。
* 负载均衡把发送方 A 分配在服务器 1，接收方 B 分配在服务器 2。当 A 给 B 发消息时，请求打在服务器 1，服务器 1 在自己的内存 Map 里根本找不到 B 的连接，这就导致无法直接将消息推送给 B。

#### Q12：Redis Pub/Sub（发布订阅）在这里起到了什么作用？
* **答**：它充当了**“跨实例的消息广播通道”**。
* 当服务器 1 本地找不到 B 的 Session 时，它就往 Redis 的广播频道发送一条推送广播。所有订阅了该频道的服务器节点都会收到这则消息，并各自在本地内存里查。只有连着用户 B 的服务器 2 能匹配成功，并调用本机的 WebSocket 完成推送。通过 Redis 广播，我们巧妙地打破了服务器间的内存壁垒。

#### Q13：多实例下为什么不用轮询，而是使用 WebSocket？
* **答**：如果使用传统的 HTTP 短轮询（前端每隔几秒请求一次后台有无新消息），不仅实时性差（延迟高），而且在高并发下，成千上万的客户端频繁空查接口，会给 Web 服务器和 MySQL 数据库带来持续不断的无效请求压力，瞬间冲垮数据库。
* 而 WebSocket 是**双向长连接，全双工通信**，一旦有新消息后端直接向前端推送，耗费资源极少，且能做到真正的毫秒级实时下发。

#### Q14：如果用户不在线，消息是怎么处理的？
* **答**：如果用户不在线，所有的服务器本地都查不到 Session，此时不会进行实时 WebSocket 推送。
* 但消息是安全的，因为在推送前它已经**落库写入了 MySQL**（初始状态为未读 `UNREAD`），并在 Redis 中给接收方的未读数计数器加 1。当用户重新上线后，前端会先调用 HTTP 接口主动把所有未读历史消息拉取出来展现并标为已读，之后便能继续接收实时的 WebSocket 推送。

#### Q15：如果此时 Redis 挂了，WebSocket 广播失效，消息怎么投递？
* **答**：如果 Redis 挂了，Pub/Sub 广播会失效。但因为消息在推送前已经**写入了 MySQL 数据库**。此时，如果接收方连在同一台服务器上，依然可以通过**本地直投**推送成功；如果是跨节点的连接，实时推送会失败，但用户重新上线、刷新网页或切换聊天框时，前端会通过 HTTP 请求重新去 MySQL 数据库里把未读消息拉出来，消息依然不会丢。

---

## 模块四：动态热帖排行

#### Q16：为什么用 Redis ZSet 而不是每次用 SQL 实时排序？
* **答**：热度分值包含了浏览、点赞、收藏等多个权重，还和时间有关。如果用 SQL 实时计算排序，因为是动态数值，**数据库无法通过建立索引来加速**，高并发下每次请求都会触发全表扫描计算，会瞬间把数据库 CPU 打满。
* 而 Redis 的 **ZSet（有序集合）** 在内存中基于跳跃表实现，调用 `ZREVRANGE` 拿前 10 名的复杂度只有 $O(\log N)$，速度极快，能极大地保护数据库。

#### Q17：既然已经实时增量加分了，为什么还要定时全量重建？
* **答**：因为我们的热度算法有**“时间半衰期衰减”**。老帖子的分值会随着发布时间的增加而自然衰退。
* 但“时间流逝”本身是不会触发 Redis 自动扣分的，如果不全量重建，老帖子就会永远占着排行榜，榜单数据就“僵死”了。所以我们设了每日凌晨全量重算来纠正时间偏差。
* **蓝绿双榜细节**：为了避免重建时先删除 Redis 老 Key 导致并发读请求拿到“空榜”，我们采用了**蓝绿双 ZSet 槽位切换机制**。平时读写 A 槽，重建时全量数据只写在未激活的 B 槽，写完后一瞬间原子切换指针到 B 槽，做到了无缝升级、高可用。

#### Q18：Redis重启后，排行榜怎么恢复？
* **答**：我们有**定时与启动自动重建任务**。监听 Spring 的 `ApplicationReadyEvent`，在系统启动完成的第一时间，自动跑一次热度全量重新计算任务，把最近 7 天的帖子分数写入 Redis，从而瞬间恢复数据。平时凌晨 3 点也定时跑重算，纠正时间流逝引起的数据偏差。

#### Q19：如果此时 Redis 挂了，热帖排行榜 ZSet 无法访问怎么处理？
* **答**：我们在代码里做了 `try-catch`。如果 Redis 挂了访问报错，就会自动降级**直接去 MySQL 数据库里查询最近 7 天的帖子数据**，在数据库里排好序返回给前端。这样虽然速度会慢一点，但至少能保证排行榜有数据，不会导致页面白屏。

---

## 模块五：AI能力模块

#### Q20：为什么要用 RabbitMQ 异步，而不是发帖时同步调用 AI 接口？
* **答**：AI 审核（文本、多图、视频校验）属于 CPU 和 network IO 密集型操作，需要调大模型和截帧，耗时可达数秒甚至十几秒。
* 如果在发帖接口中**同步调用**，会导致发帖接口被长时间阻塞，只要有几十人并发发帖，Tomcat 线程池就会被打满，导致整个服务卡死雪崩。引入 **RabbitMQ 异步解耦**后，发帖时只写 MySQL 并发个消息到 MQ 就能给用户返回成功，AI 审核在后台异步慢慢跑，极大提升了发帖吞吐量和稳定性。

#### Q21：LangGraph 在这里具体做了什么？（简单回答）
* **答**：LangGraph 在这里起到了**“AI 审核工作流编排和状态管理”**的作用。
* 它把我们的审核流程编排成了一个有逻辑判定分支的流水线：第一步审**文本**，若文本违规直接走向 Reject（驳回）节点结束，不浪费算力去下载和校验图片；若文本通过，再进**图片**和**视频**校验；全部合规后，最后调用 LLM 自动提取文章**内容摘要**并 APPROVED（通过）放行。这种模式简化了多模态 AI 交互的控制逻辑，代码复用性很强。

#### Q22：如果 RabbitMQ 挂了，异步 AI 审核任务发不出去怎么办？
* **答**：我们通过**本地消息表和定时任务扫表**来兜底处理：
  1. **备份入库**：发帖时，在同一个 MySQL 事务里，往本地消息表里同步插入一条待发送的任务记录。就算发 MQ 失败，记录依然安全保存在 MySQL 数据库里。
  2. **定时任务重投**：后端写了一个定时任务，每隔 1 分钟去扫描一次本地消息表。只要发现有未发送成功的审核任务，就尝试重新投递，一旦等 RabbitMQ 服务恢复，积压的任务就会自动全部补发。
  3. **超时处理**：如果超过 10 分钟帖子还是待审核状态，定时任务会把它挑出来重新投递重试，或者转入后台的人工审核去处理，防止帖子永久卡死。

---

# 第二部分：💻 核心源码对照

## 模块一：验证码防刷源码

#### 1. Redis 原子校验与销毁（Lua 脚本）
在 `RedisAtomicValueConsumer.java` 中，确保校验与删除是一步原子操作：

```java
// Lua 脚本：先 GET 读值，一致则 DEL，整个过程由 Redis 保证单线程原子执行
private static final DefaultRedisScript<Long> CONSUME_SCRIPT = new DefaultRedisScript<>(
        "local val = redis.call('GET', KEYS[1]) "
                + "if not val then return 0 end "
                + "if val ~= ARGV[1] then return 0 end "
                + "redis.call('DEL', KEYS[1]) "
                + "return 1",
        Long.class);

public static boolean consumeIfMatch(StringRedisTemplate redis, String key, String expected) {
    // 执行 Lua 脚本，若返回 1L 则代表校验通过且成功销毁
    Long result = redis.execute(CONSUME_SCRIPT, Collections.singletonList(key), expected);
    return result != null && result == 1L;
}
```

#### 2. 发码限流逻辑
在 `SMSCodeServiceImpl.java` 中，实现了 60 秒冷却限流与 30 分钟最多 10 次的限流：

```java
private void sendInternal(String phoneNumber, String prefix) {
    // 1. 校验 60 秒冷却（Cooldown Key 是否存在）
    String cooldownKey = Constant.REDIS_KEY_SMS_COOLDOWN + phoneNumber;
    if (stringRedisTemplate.hasKey(cooldownKey)) {
        throw new ApplicationException(Result.fail(ResultCode.FAILED_SMS_RATE_LIMIT));
    }

    // 2. 校验 30 分钟累计次数（限制最多 10 次）
    String countKey = Constant.REDIS_KEY_SMS_COUNT + phoneNumber;
    Long count = stringRedisTemplate.opsForValue().increment(countKey);
    if (count != null && count == 1) {
        stringRedisTemplate.expire(countKey, 1800, TimeUnit.SECONDS); // 第一次发码，设置 30 分钟 TTL
    }
    if (count != null && count > 10) {
        throw new ApplicationException(Result.fail(ResultCode.FAILED_SMS_RATE_LIMIT));
    }

    // 3. 生成验证码并写入 Redis，同时写入 60 秒冷却 Cooldown Key
    String code = CaptchaUtils.getCapthca(4);
    stringRedisTemplate.opsForValue().set(prefix + phoneNumber, code, 60, TimeUnit.SECONDS);
    stringRedisTemplate.opsForValue().set(cooldownKey, "1", 60, TimeUnit.SECONDS);

    // 4. 调用短信通道发送（若异常则 delete 掉已写入的 Redis 键）
    // ...
}
```

---

## 模块二：并发抽奖控制源码

#### 1. 事务内悲观锁锁定用户
在 `LotteryServiceImpl.java` 中，针对用户扣减积分进行并发保护：

```java
@Override
@Transactional(rollbackFor = Exception.class)
public LotteryDrawResultVO draw(Long userId, LotteryDrawDTO dto) {
    // 1. 悲观行锁：在事务内锁定当前用户行，防止并发下余额重复扣减
    User lockedUser = userMapper.selectByIdForUpdate(userId);
    
    // 2. 扣减用户积分
    pointsService.deductPoints(userId, totalCost, ...);

    // 3. 循环执行单次抽奖（传入保底标识）
    for (int i = 0; i < times; i++) {
        LotteryDrawItemVO item = executeOneDraw(userId, activityId, ..., forceGrand, forceRareOnly);
    }
}
```

#### 2. 条件更新扣减库存与重试循环
在 `LotteryServiceImpl.java` 中，乐观条件更新与 64 次换奖重试机制：

```java
private LotteryDrawItemVO executeOneDraw(Long userId, Long activityId, ...) {
    for (int attempt = 0; attempt < MAX_STOCK_RETRY; attempt++) { // 最多尝试 64 次
        // 1. 重新从数据库拉取当前所有还有库存的奖品池
        List<LotteryPrizePoolRow> available = pool.stream().filter(this::hasStock).collect(...);
        
        // 2. 根据保底策略过滤候选池，并进行加权随机
        List<LotteryPrizePoolRow> candidate = resolveCandidatePool(available, forceGrand, forceRareOnly);
        LotteryPrizePoolRow picked = weightedPick(candidate);
        
        // 3. 执行条件更新扣减库存（不满足 stock > 0 则更新影响行数为 0）
        int ok = lotteryActivityPrizeMapper.decrementStockIfPositive(picked.getActivityPrizeId());
        if (ok != 1) {
            continue; // 扣减失败，继续下一次循环（换奖重抽）
        }
        return persistDraw(userId, activityId, picked, batchKey); // 扣减成功，入库返回
    }
    throw new ApplicationException(Result.fail(ResultCode.ERROR_SERVICES));
}
```

#### 3. 保底候选池过滤
在 `LotteryServiceImpl.java` 中实现奖品池强行缩容过滤：

```java
private List<LotteryPrizePoolRow> resolveCandidatePool(List<LotteryPrizePoolRow> available,
                                                       boolean forceGrand, boolean forceRareOnly) {
    if (forceGrand) {
        // 硬保底：只留下大奖进行随机
        return available.stream().filter(r -> r.getIsJackpot() == 1).collect(Collectors.toList());
    }
    if (forceRareOnly) {
        // 软保底：只留下稀有奖品进行随机
        return available.stream().filter(LotteryServiceImpl::isSoftPityRareRow).collect(Collectors.toList());
    }
    return available;
}
```

---

## 模块三：私信跨实例推送源码

#### 1. 优先本地直投，失败再发 Redis 广播
在 `WebSocketPushService.java` 中对广播通知进行本地优化：

```java
public void push(Long userId, String payload) {
    // 1. 本地直投优化：如果目标用户连在当前节点，直接发送，无需网络和广播开销
    boolean delivered = pushLocal(userId, payload);
    if (delivered) {
        return; 
    }
    
    // 2. 本地未命中，说明在其他节点，向 Redis 频道发送订阅广播
    try {
        String envelope = objectMapper.writeValueAsString(Map.of("userId", userId, "payload", payload));
        stringRedisTemplate.convertAndSend(Constant.WS_PUSH_CHANNEL, envelope);
    } catch (Exception e) {
        log.warn("WS 推送发布失败", e);
    }
}

public boolean pushLocal(Long userId, String payload) {
    // 从本机内存的 Session 注册表查出并发送
    return onlineUserManageUtil.sendMessage(userId, payload);
}
```

#### 2. 同步落库与异步推送解耦
在 `MessageServiceImpl.java` 中确保发送行为先行落表：

```java
@Override
@Transactional(rollbackFor = Exception.class)
public MessageVO send(SendMessageRequest req, Long sendUserId) {
    // 1. 消息落库写入 MySQL (状态设为 UNREAD)
    Message newMessage = new Message();
    newMessage.setPostUserId(sendUserId);
    newMessage.setReceiveUserId(receiveUserId);
    newMessage.setState(Constant.MESSAGE_STATE_UNREAD);
    messageMapper.insert(newMessage);
    
    // 2. 在事务成功 Commit 之后，再执行异步推送通知
    publishNotify(newMessage.getId(), sendUserId, receiveUserId, ...);
    
    // 3. Redis 累加接收方未读数
    incrementUnreadCount(receiveUserId, 1L);
    return ...;
}
```

---

## 模块四：动态热帖排行源码

#### 1. 蓝绿双 ZSet 槽位切换
在 `HotArticleRedisOps.java` 中，避免重建时的空榜抖动：

```java
public void rebuildBlueGreen(Map<String, Double> memberScores) {
    // 1. 获取当前未激活的槽位（例如当前读写 B 槽，则获取 A 槽）
    String inactiveKey = resolveInactiveKey();
    stringRedisTemplate.delete(inactiveKey); // 清空未激活槽位
    
    // 2. 将全量重新计算的最新分数写入未激活槽位
    if (memberScores != null && !memberScores.isEmpty()) {
        for (Map.Entry<String, Double> entry : memberScores.entrySet()) {
            stringRedisTemplate.opsForZSet().add(inactiveKey, entry.getKey(), entry.getValue());
        }
    }
    
    // 3. 原子更新指针，将并发读写流量一瞬间切换到新槽位上
    String newPointer = ForumRedisKeys.HOT_ARTICLES_SLOT_A.equals(inactiveKey) ? POINTER_A : POINTER_B;
    stringRedisTemplate.opsForValue().set(ForumRedisKeys.HOT_ARTICLES_ACTIVE, newPointer);
}
```

#### 2. 定时与启动自动重建
在 `HotArticleRankingTask.java` 中，保障缓存崩塌重启后的自愈：

```java
// 每天凌晨 3 点重算，避开业务高峰，纠正时间漂移
@Scheduled(cron = "0 0 3 * * ?")
public void rebuildDaily() {
    articleService.rebuildHotArticleRanking();
}

// 容器启动完成后跑一次，确保 Redis 重启后榜单立即恢复
@EventListener(ApplicationReadyEvent.class)
public void rebuildOnStartup() {
    articleService.rebuildHotArticleRanking();
}
```

---

## 模块五：AI能力模块源码

#### 1. MQ 发送审核任务与监听结果
在 Java 端执行解耦发码：

```java
// 1. 发送端：发帖成功后异步投递任务，使发帖主流程与审核解耦
public void sendArticleAuditTask(Object task) {
    send(auditRabbitTemplate, Constant.ROUTING_KEY_AUDIT_TASK, task);
}

// 2. 接收端：监听 AI 异步处理完的审核结果，更新帖子状态
@RabbitListener(queues = Constant.QUORUM_QUEUE_AUDIT_RESULT, ackMode = "MANUAL")
public void handleArticleAuditResult(Message message, Channel channel) throws IOException {
    ArticleAuditResultMqVO vo = objectMapper.readValue(message.getBody(), ArticleAuditResultMqVO.class);
    articleService.applyAuditResult(vo); // 上架或驳回
    channel.basicAck(message.getMessageProperties().getDeliveryTag(), false);
}
```

#### 2. LangGraph 状态机图的定义与路由
在 `article_audit.py` 中，定义 Python 后端的多节点工作流：

```python
def build_graph() -> StateGraph:
    g = StateGraph(AuditState)
    
    # 1. 注册工作流中的所有状态节点
    g.add_node("validate_text", node_validate_text)
    g.add_node("validate_images", node_validate_images)
    g.add_node("validate_video", node_validate_video)
    g.add_node("summarize", node_summarize)
    g.add_node("reject_text", node_to_rejected_text)
    # ...
    g.add_node("finalize", node_finalize)

    # 2. 编排流转边缘与条件分支
    g.add_edge(START, "validate_text")
    
    # 文本校验完毕后的路由条件：违规走文本驳回，通过走图片校验，异常走报错
    g.add_conditional_edges("validate_text", route_after_text, {
        "pass": "validate_images",
        "reject": "reject_text",
        "error": "error",
    })
    
    # 图片校验完毕后的路由条件：通过无视频走摘要，有视频走视频校验，不合规走图片驳回
    g.add_conditional_edges("validate_images", route_after_images, {
        "pass": "summarize",
        "video": "validate_video",
        "reject": "reject_image",
        "error": "error",
    })
    # ...
    g.add_edge("summarize", "approved")
    g.add_edge("finalize", END)
    return g
```
