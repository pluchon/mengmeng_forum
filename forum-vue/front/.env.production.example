# 生产构建可选配置（复制为 .env.production 后 npm run build）
# 留空则自动使用当前站点：wss://<host>/ws/notify

# 错误示例（会导致 //notify 且缺少 /ws）：
# VITE_WS_BASE_URL=wss://www.nuonuoya.cn/

# 正确示例（一般无需设置，走同源即可）：
# VITE_WS_BASE_URL=wss://www.nuonuoya.cn
