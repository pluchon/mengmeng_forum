# 生产构建可选配置（复制为 .env.production 后 npm run build）
# HTTP API / WebSocket 均经 Gateway 同源反代；留空则自动使用当前站点。

# WebSocket 正确示例（一般无需设置，走同源 /ws/notify）：
# VITE_WS_BASE_URL=wss://www.example.com
# 错误示例（会导致 //notify 且缺少 /ws）：
# VITE_WS_BASE_URL=wss://www.example.com/
